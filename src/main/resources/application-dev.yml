spring:
  devtools:
    restart:
      enabled: true  #设置开启热部署
    freemarker:
      cache: false
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/moguboot?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
#debug: true
server:
  port: 8086
